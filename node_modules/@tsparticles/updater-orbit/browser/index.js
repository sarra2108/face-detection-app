export async function loadOrbitUpdater(engine, refresh = true) {
    await engine.addParticleUpdater("orbit", async (container) => {
        const { OrbitUpdater } = await import("./OrbitUpdater.js");
        return new OrbitUpdater(container);
    }, refresh);
}
